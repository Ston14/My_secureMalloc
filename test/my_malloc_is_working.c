#include <stdio.h>

int main(){
    /*faire fonctionner l'appel a malloc*/
    /*retoutner un mmap failed*/
    /*retourner un mmap reussi */
    if(my_malloc() == NULL){
        printf("not passed");
    }
}